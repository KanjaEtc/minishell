#include "minishell.h"

char	*strip_quotes(char *str)
{
	char	*new;
	int		i;

	new = malloc(sizeof(char) * (ft_strlen(str) - 1));
	i = 0;
	while (str[i - 1])
	{
		new = 
	}


}

void	strip_all_tokens(t_token *tokens)
{

}